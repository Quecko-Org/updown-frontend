import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Privacy Policy | PulsePairs" };

export default function PrivacyPage() {
  return (
    <PlaceholderPage
      title="Privacy Policy"
      subtitle="PulsePairs is a non-custodial protocol — wallet addresses, on-chain trades, and market data are public. This page details what additional data is collected and how it is handled."
    />
  );
}
